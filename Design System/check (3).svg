<!-- @dsCard group="Type" viewport="700x240" name="Subhead & Body" subtitle="Archivo 400 · subhead lh 1.34 · body lh 1.75 · max measure 780px" -->
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="stylesheet" href="../styles.css">
<style>
  body { margin: 0; padding: 24px 28px; background: var(--cream); }
  .label { font-family: var(--font-mono); font-size: 10px; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: var(--teal); margin-bottom: 6px; }
  .spec  { font-family: var(--font-mono); font-size: 10px; color: var(--muted); margin-top: 4px; }
</style>
</head>
<body>
<div class="label">Subhead</div>
<div style="font-family:var(--font-serif);font-size:clamp(18px,2.25vw,28px);font-weight:400;line-height:1.34;color:var(--ink);text-wrap:balance;max-width:600px">We don't hand off a system and walk away. We run it — every day, for the life of the room.</div>
<div class="spec">Archivo 400 · size clamp(21px → 31px) · lh 1.34</div>
<div class="label" style="margin-top:18px">Body Copy</div>
<div style="font-family:var(--font-sans);font-size:clamp(16px,1.1vw,18px);font-weight:400;line-height:1.75;color:var(--slate);max-width:580px">The AV industry defaults to reactive support: something breaks, someone calls, someone comes to fix it. IMS was built around a different model — proactive, monitored, accountable.</div>
<div class="spec">Archivo 400 · size clamp(16px → 18px) · lh 1.75 · color --slate · measure max 780px</div>
</body>
</html>
